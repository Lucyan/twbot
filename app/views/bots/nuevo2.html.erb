<% provide(:title, 'Nuevo FollowKeyword') %>
<div class="center hero-unit">
  	<h1>Nuevo Followkeyword</h1>
	<%= render "layouts/menu" %>
  	<h2>Si esta no es la cuenta que deseas utilizar, <a href="https://twitter.com/logout" target="_blank">deslogeate</a> de twitter en otra ventana y <%=link_to "vuelve a empezar", bot_nuevo_path %></h2>
  	<div class="form_puntos origen">
		<%= form_for @bot, :url => bot_nuevo_path do |form| %>
			<fieldset class="fields">
				<div class="field control-group">
					<%= form.label :nombre %>
					<%= form.text_field :nombre %>
				</div>

				<div class="field control-group">
					<%= form.label :tw_cuenta, "Cuenta Twitter" %>
					<div class="input-prepend">
						<span class="add-on">@</span><%= form.text_field :tw_cuenta, {class: "span2", readonly: "readonly"} %>
					</div>
				</div>

				<div style="display:none;" class="field control-group">
					<%= form.label :tw_token, "Twitter Token" %>
					<%= form.text_field :tw_token, {class: "input-xxlarge", readonly: "readonly"} %>
				</div>

				<div style="display:none;" class="field control-group">
					<%= form.label :tw_secret, "Twitter Secret" %>
					<%= form.text_field :tw_secret, {class: "input-xxlarge", readonly: "readonly"} %>
				</div>

				<div class="field control-group">
					<%= form.label :cantidad_seguir, "Máximo a seguir por hora" %>
					<%= form.text_field :cantidad_seguir, {value: "10"} %>
				</div>

				<div class="field control-group">
					<%= form.label :verificar_seguido, "Verificar seguidos despues de" %>
					<div class="input-prepend">
						<%= form.text_field :verificar_seguido, {class: "span1", value: "2"} %><span class="add-on">Dias</span>
					</div>
				</div>
			</fieldset>

			<fieldset class="actions">
				<%= form.submit "Guardar", class: "btn btn-large btn-primary" %>
			</fieldset>
		<% end %>
	</div>
</div>